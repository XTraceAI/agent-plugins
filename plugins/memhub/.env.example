# Install this file at ~/.config/memhub-plugin/.env with mode 0600, then
# replace the placeholder with a long, unique passphrase. Never commit the
# populated file. Quote the value when it contains spaces or # characters.
MEMHUB_ENCRYPTION_PASSPHRASE="replace-with-a-long-unique-passphrase"
